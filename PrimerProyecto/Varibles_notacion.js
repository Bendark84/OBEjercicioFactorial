var variable; // ya no se usa mucho
let varibalelet;

const constante = 'Hola';

//notacion
//;
const b = 4;
b + 4;

// . se utiliza en los objetos para acceder a los atributos
//movil.anchura

//[] => listas, arreglos o arrays
const ar = [1, 2, 3, 4];
//() => Funciones
function suma(a, b) {}
//{} => objetos, funciones y estructuras de control
const movil = {
  anchura: 5,
  altura: 10,
};
if (true) {
  //todo lo que este ntre llaves
}
